{
  "name": "intel-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Advanced Intelligence Management and Collection Platform",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "prod": "npm run build && npm run start",
    "prod:seed": "node scripts/seed-production.js",
    "db:migrate": "npm run db:migrate --prefix server",
    "db:rollback": "npm run db:rollback --prefix server",
    "db:seed": "npm run db:seed --prefix server",
    "db:refresh": "npm run db:rollback --prefix server && npm run db:migrate --prefix server && npm run db:seed --prefix server",
    "module:create": "node scripts/create-module.js",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.18.1"
  }
}
